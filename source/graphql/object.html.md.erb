---
title: "GraphQL Object"
---

#  <%= obj.name %>
<%= obj.description %>

## Interfaces
<%= obj.interfaces.sort_by { |i| i.name}.map { |i| type_link(i) }.join(', ') %>

## Fields

<% obj.fields.sort_by { |f| f.name }.each do |field| %>

**<%= field.name %>**(<%= field.args.map { |a| "#{a.name}: #{type_link(a.type)}"}.join(' ')%>): <%= field_link(field) %>

<%= field.description %>
<% end %>
